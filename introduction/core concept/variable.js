/**
 * variables:- variables are used to store data.
 * variables are like containers used for storing data values. 
 */

// variable syntax 

// defining a number varible 
 
let noOfStudents = 30;
console.log(noOfStudents);

// defining a string valriable 

let welcomeMessage = "Hello Amaan";
console.log(welcomeMessage);

/**
 * In variable:- Can't use keyword or reserved words. 
 * Can't start with a number.
 * No other special character other than $ and _.
 * = is for assignment.
 * ; this means ends of instruction. 
 */

 // eg 

 let money = 100;
 console.log(money);

 // Variables updating values 

 let numberOfStudent = 10;
//  numberOfStudent = numberOfStudent + 1;
//  console.log(numberOfStudent) // 11

 let $money = 1 ;
 $money += 5;
 console.log($money); // 6

 /**
  * syntax for updating varibles values 
  * 1. Do not need to use let again 
  * syntax: variable = variable +1;
  * Assignment opertator is used = 
  * Short hand assignment operators: 
  * += , -= , *= , /= , ++ 
  */


 console.log("------------------------------------------------------------------")
 numberOfStudent = numberOfStudent + 40;
 console.log(numberOfStudent); 


 // exapmle of raduis 
 let radius = 4;
  let area = 3.14 * radius * radius;
  console.log(area);

  const PI = 3.14;  
  let parameter = 2 * PI * radius;
  console.log(parameter);



